{
  "name": "@workout/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "web·apps/api가 공유하는 프레임워크-무지 도메인/인프라 코드 (source-only TS package)",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "DATABASE_URL=\"${DATABASE_URL:-postgres://test:test@127.0.0.1:5432/test}\" tsx --test $(find src -name '*.test.ts' | sort)",
    "lint:boundary": "! grep -rnE 'from \"(@/|next/|react|@workout/api)' src --include='*.ts' || (echo 'core 경계 위반: core는 @/(web)·next·react를 import할 수 없습니다' && exit 1)"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "8.16.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "drizzle-orm": "0.45.1",
    "pg": "8.18.0",
    "zod": "^3.25.76"
  }
}
